#!/bin/bash

. $HOME/.config/admiral.d/config
msg=`python ~/.local/bin/gmail.py`
if [[ $msg =~ .*0.*  ]] || [[ $msg == '' ]]
then
     echo "%{F$GREEN}[ %{F$MAGENTA}%{F$FG} ${msg}%{F$GREEN} ] "
else
     echo "%{F$GREEN}[ %{F$RED}%{F$FG} ${msg}%{F$GREEN} ] "
fi
