#!/bin/bash

. $HOME/.config/admiral.d/config
msg=`python ~/.local/bin/gmail.py`
if [[ $msg =~ .*0.*  ]] || [[ $msg == '' ]]
then
     echo " %{F$BLUE}%{F$BLUE} ${msg} "
else
     echo " %{F$GREEN}%{F$FG} ${msg} "
fi
