#!/bin/bash

. $HOME/.config/admiral.d/config
msg=`python ~/.local/bin/gmail.py`
if [[ $msg =~ .*0.*  ]] || [[ $msg == '' ]]
then
     echo "%{B$RED} %{F$BBLACK} %{B-}%{F$FG} Mail: ${msg} "
else
     echo "%{B$RED} %{F$BBLACK} %{B-}%{F$FG} Mail: ${msg} "
fi
