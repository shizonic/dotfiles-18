#!/bin/bash

. $HOME/.config/admiral.d/config
networkIcon=""

if ping -c 1 google.com >> /dev/null 2>&1; then
	echo "%{B$BLUE} %{F$BBLACK}${networkIcon} %{B-} %{F$FG}Signal: Online "
else
	echo "%{B$BLUE} %{F$BBLACK}${networkIcon} %{B-} %{F$FG}Signal: Offline "
fi	
