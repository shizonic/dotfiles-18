#!/bin/bash

. $HOME/.config/admiral.d/config
networkIcon=""

if ping -c 1 google.com >> /dev/null 2>&1; then
	echo "%{B$BLACK} %{F$BBLUE}${networkIcon} %{B-} %{F$FG}Online "
else
	echo "%{B$BLACK} %{F$FG}${networkIcon} %{B-} %{F$FG}Offline "
fi	
